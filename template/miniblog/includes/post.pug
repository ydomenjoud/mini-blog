
header
	if post.image
		img(src=post['article_directory'] + post['image'], align="right")
	h1.title
		a(href=post.url)=post.title
.entry
	p #{post.description}
		a.more(href=post.url) see more
footer
	if post.tags
		each tag in post.tags
			a(href="/search.html?tags=" + tag)='#' + tag
			|  &nbsp;
		| / &nbsp;
	if post.author
			a(rel="author", href="/search.html?author=" + post['author'] )=post.author
			| &nbsp;/&nbsp;
			time(datetime=post.publish.timestamp)=post.publish.formatted
.clearfix