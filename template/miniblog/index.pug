extends layout

block content
   each post in pages_list
      article.post
         .post-content
            header
               h1.title
                  a(href=post.url)=post.title
               time(datetime=post.publish)=post.publish
            .entry
               p #{content}
            .clearfix